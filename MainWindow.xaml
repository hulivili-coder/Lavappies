<Window x:Class="Lavappies.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lavappies"
        mc:Ignorable="d"
        Title="Lavappies" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center">
            <Image x:Name="LogoImage" Width="32" Height="32" Margin="5"/>
            <TextBlock Text="Lavappies" FontSize="24" FontWeight="Bold" Margin="5"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Content="Add App" Click="AddApp_Click" Margin="5"/>
            <Button Content="Remove Selected" Click="RemoveApp_Click" Margin="5"/>
            <Button Content="Launch Selected" Click="LaunchApp_Click" Margin="5"/>
            <Button Content="Create Portable EXE" Click="CreatePortableExe_Click" Margin="5"/>
            <CheckBox x:Name="AutoStartCheckBox" Content="Auto-start on login" Checked="AutoStartCheckBox_Checked" Unchecked="AutoStartCheckBox_Unchecked" Margin="5"/>
            <CheckBox x:Name="DarkModeCheckBox" Content="Dark Mode" Checked="DarkModeCheckBox_Checked" Unchecked="DarkModeCheckBox_Unchecked" Margin="5"/>
        </StackPanel>
        <ListView x:Name="AppsListView" Grid.Row="2">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Icon" Width="50">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Icon}" Width="32" Height="32"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path}" Width="400"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
